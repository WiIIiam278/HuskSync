name: Release Tests & Publish

on:
  release:
    types: [ 'published' ]

permissions:
  contents: read
  checks: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout for CI 🛎️'
        uses: actions/checkout@v7
      - name: 'Set up JDK 25 📦'
        uses: actions/setup-java@v6
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: 'Set up Gradle 📦'
        uses: gradle/actions/setup-gradle@v6.3.0
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/master' }}
      - name: 'Build with Gradle 🏗️'
        run: ./gradlew build test publish --build-cache
        env:
          RELEASES_MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          RELEASES_MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
      - name: 'Publish Test Report 📊'
        uses: mikepenz/action-junit-report@v6
        if: success() || failure() # Continue on failure
        with:
          report_paths: '**/build/test-results/test/TEST-*.xml'
      - name: 'Publish to William278.net 🚀'
        uses: WiIIiam278/bones-publish-action@v1
        with:
          api-key: ${{ secrets.BONES_API_KEY }}
          project: 'husksync'
          channel: 'release'
          version: ${{ github.event.release.tag_name }}
          changelog: ${{ github.event.release.body }}
          distro-names: |
            paper-1.21.10
            paper-1.21.11
            paper-26.1.2
            paper-26.2
            paper-26.3
          distro-groups: |
            paper
            paper
            paper
            paper
            paper
          distro-descriptions: |
            Paper 1.21.10
            Paper 1.21.11
            Paper 26.1.2
            Paper 26.2
            Paper 26.3
          files: |
            target/HuskSync-Bukkit-${{ github.event.release.tag_name }}+mc.1.21.10.jar
            target/HuskSync-Bukkit-${{ github.event.release.tag_name }}+mc.1.21.11.jar
            target/HuskSync-Bukkit-${{ github.event.release.tag_name }}+mc.26.1.2.jar
            target/HuskSync-Bukkit-${{ github.event.release.tag_name }}+mc.26.2.jar
            target/HuskSync-Bukkit-${{ github.event.release.tag_name }}+mc.26.3.jar
